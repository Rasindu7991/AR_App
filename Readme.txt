* Open the terminal and run the below command
    npm install -g local-web-server

*Go to the source folder and Open terminal from that directory and 
Run the app by using below command
    ws --https
